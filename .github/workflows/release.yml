name: Build and release image

on:
  push:
    branches: [main, offline-install]   # Main instead of release, because users access /pub directly on main branch

defaults:
  run:
    shell: bash

jobs:
  build_intel:
    runs-on: ubuntu-24.04
    steps:
      - name: Make the files in this commit available to the current directory
        uses: actions/checkout@v4
        with:
          path: ./repo/

      - name: Build image
        id: build_image
        uses: ./repo/.github/actions/build
        with:
          artifact_name: image-x86_64

      - name: Debug output
        if: always()
        uses: ./repo/.github/actions/debug_output
    outputs:
      artifact_id: ${{ steps.build_image.outputs.artifact_id }}
      filename: ${{ steps.build_image.outputs.filename }}

  build_arm:
    runs-on: ubuntu-24.04-arm
    steps:
      - name: Make the files in this commit available to the current directory
        uses: actions/checkout@v4
        with:
          path: ./repo/

      - name: Build image
        id: build_image
        uses: ./repo/.github/actions/build
        with:
          artifact_name: image-aarch64

      # - name: Debug output
      #   if: always()
      #   uses: ./repo/.github/actions/debug_output
    outputs:
      artifact_id: ${{ steps.build_image.outputs.artifact_id }}
      filename: ${{ steps.build_image.outputs.filename }}

  create_release:
    runs-on: ubuntu-latest
    permissions:
      contents: write # allow workflow to create releases
    steps:
      - name: Create short version of commit SHA-1
        id: truncated_sha
        uses: 2428392/gh-truncate-string-action@v1
        with:
          stringToTruncate: ${{ github.sha }}
          maxLength: 7

      - name: Create Version Name
        id: version_name
        run: echo "id=$VERSION_ID" >> "$GITHUB_OUTPUT"
        env:
          VERSION_ID: v0.2.${{ steps.truncated_sha.outputs.string }}
        # output:
        # id

      - name: Create Release
        id: create_release
        uses: actions/create-release@latest
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          VERSION_ID: v0.2.${{ steps.truncated_sha.outputs.string }}
        with:
          tag_name: ${{ env.VERSION_ID }}
          release_name: ${{ env.VERSION_ID }}
          draft: false
          prerelease: false
        # outputs:
        # id: The release ID
        # html_url: The URL users can navigate to in order to view the release.
        # upload_url: The URL for uploading assets to the release.
    outputs:
      upload_url: ${{ steps.create_release.outputs.upload_url }}
      name: ${{ steps.version_name.outputs.id }}
      release_id: ${{ steps.create_release.outputs.id }}

  attach_release_files:
    runs-on: ubuntu-latest
    needs:
      - build_intel
      - build_arm
      - create_release
    permissions:
      contents: write # allow workflow to edit release
    steps:
      - name: Make the files in this commit available to the current directory
        uses: actions/checkout@v4
        with:
          path: ./repo/

      - name: Download artifact from build_images
        id: get_artifact
        uses: actions/download-artifact@v4
        with:
          pattern: image-*
          merge-multiple: true
          path: ~/images
        # outputs:
        # download-path

      - name: Upload Release Files (1/2)
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ needs.create_release.outputs.upload_url }}
          asset_path: ${{ format( '{0}/{1}/{2}',
                            steps.get_artifact.outputs.download-path,
                            'x86_64',
                            needs.build_intel.outputs.filename
                        ) }}
          asset_name: ${{ format('ITSC-3146-Lima-{0}.img.gzip', 'x86_64') }}
          asset_content_type: application/gzip

      - name: Upload Release Files (2/2)
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ needs.create_release.outputs.upload_url }}
          asset_path: ${{ format( '{0}/{1}/{2}',
                            steps.get_artifact.outputs.download-path,
                            'aarch64',
                            needs.build_intel.outputs.filename
                        ) }}
          asset_name: ${{ format('ITSC-3146-Lima-{0}.img.gzip', 'aarch64') }}
          asset_content_type: application/gzip

      - name: Debug output
        if: always()
        uses: ./repo/.github/actions/debug_output

  delete_release_if_fail:
    runs-on: ubuntu-latest
    needs:
      - build_intel
      - build_arm
      - create_release
      - attach_release_files
    if: |
      always() &&
      ( contains(needs.*.result, 'failure') || 
        contains(needs.*.result, 'cancelled') )
    permissions:
      contents: write # allow workflow to edit release
    steps:
      - name: Delete Release (if failure occurred)
        uses: liudonghua123/delete-release-action@v1
        with:
          release_id: ${{ needs.create_release.outputs.release_id }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Delete tag (if failure occurred)
        run: gh release delete $VERSION_ID --cleanup-tag
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          VERSION_ID: v0.2.${{ needs.create_release.outputs.name }}
